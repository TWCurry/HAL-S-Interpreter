  SIMPLE: PROGRAM;
  SQUARE: FUNCTION(INPUT) SCALAR;
  RETURN INPUT**2;
  CLOSE SQUARE;
  DECLARE INPUTNO SCALAR 0;
  DECLARE OUTPUT SCALAR 0;
  DO FOR I = 1 TO 100;
  READ(1) INPUTNO;
  OUTPUT = SQUARE(INPUTNO);
  WRITE(2) OUTPUT;
  END;
  CLOSE SIMPLE;
